# CIFAR-10-neural-networks-comparisons
This project explores and compares multiple neural network architectures for image classification using the CIFAR-10 dataset. Implemented in Python with TensorFlow/Keras, the project shows a structured experimental approach to analyze how model complexity, depth and architecture impact accuracy, overfitting, and generalization.
